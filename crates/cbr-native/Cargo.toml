[package]
name = "silo-cbr-native"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "MIT OR Apache-2.0"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
compcol = { version = "=0.6.11", default-features = false, features = ["rar3", "rar5"] }
