[package]
name = "silo-rar-guest"
version = "0.1.0"
edition = "2024"
[lib]
crate-type = ["cdylib"]
[dependencies]
rars = { version = "=0.9.4", default-features = false }
serde_json = "1"
[profile.release]
panic = "abort"
opt-level = "s"
strip = true
lto = true
